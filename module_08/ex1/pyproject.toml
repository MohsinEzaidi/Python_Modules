[tool.poetry]
name = "Python_Module_08_env"
version = "1.0.0"
package-mode = false

[tool.poetry.dependencies]
python = "^3.10"
pandas = "2.1.0"
requests = "2.31.0"
matplotlib = "3.7.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"